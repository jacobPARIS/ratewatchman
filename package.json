{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic CRUD app",
  "scripts": {
    "start": "cross-env NODE_ENV=testing npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "^1.9.5",
    "cross-env": "^7.0.2"
  },
  "dependencies": {}
}
